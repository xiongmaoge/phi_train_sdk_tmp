Metadata-Version: 2.4
Name: phi-train-slurm
Version: 0.1.0
Summary: phi train infra sdk: submit and manage VLA pretraining jobs on EHPC-Slurm clusters
Requires-Python: >=3.10
Description-Content-Type: text/markdown
Provides-Extra: dev
Requires-Dist: pytest>=7; extra == "dev"
Provides-Extra: yaml
Requires-Dist: pyyaml>=6; extra == "yaml"

# phi_train_slurm

训练 infra sdk：在 EHPC-Slurm 集群上提交、查询、管理**具身 VLA 模型预训练**作业。

与 [phi_data_sdk](https://github.com/phi-robotics/phi_data_sdk) 分工：

- **phi_data_sdk（数据侧）**：数据预处理、OSS -> CPFS 数据同步（phi-sync，NAS 数据流动 OpenAPI）
- **phi_train_slurm（训练侧，本仓库）**：把训练任务提交到 Slurm、查询状态、拉日志、取消、故障自动续训

## 核心抽象

```
JobSpec (声明式配置)  →  SlurmBackend (执行器)  →  JobHandle (作业句柄)
  YAML 描述训练           sbatch/squeue/sacct        wait / logs / cancel
  长什么样                唯一碰 Slurm 的地方          操作一个已提交作业
```

```python
from phi_train_slurm import JobSpec, SlurmJobManager, SlurmBackend

spec = JobSpec.from_yaml("examples/exp_vla.yaml")
manager = SlurmJobManager(SlurmBackend(host="login01"))

job = manager.submit(spec)     # spec -> sbatch 脚本 -> sbatch -> JobHandle
job.wait(timeout=72 * 3600)    # 阻塞到结束（含故障自动重提）
print(job.status)              # SUCCEEDED / FAILED / ...
```

## 整体结构

| 路径 | 说明 |
| --- | --- |
| `model/resource.py` | `ResourceSpec`：Slurm 资源申请（partition / nodes / gpus / time_limit...） |
| `job/job_spec.py` | `JobSpec`：作业声明式配置 + YAML 解析 + 校验 + 序列化 |
| `job/status.py` | `JobStatus` 状态枚举 + `squeue`/`sacct` 原始状态 → SDK 状态归一映射 |
| `job/manager.py` | `SlurmJobManager`：对上层暴露 submit / status / logs / cancel |
| `job/handle.py` | `JobHandle`：wait（轮询+requeue 自动续训）/ logs / cancel |
| `slurm/backend.py` | `SlurmBackend`：登录节点 SSH，封装 sbatch / squeue / sacct / scancel |
| `slurm/launcher.py` | srun+torchrun 多机启动命令生成 + sbatch 脚本渲染 |
| `slurm/templates/sbatch.sh.tpl` | sbatch 脚本体模板（string.Template，零依赖） |
| `submitter/` | CLI（`phi-train`）与提交侧配置（登录节点地址等从环境变量读取） |
| `examples/exp_vla.yaml` | VLA 预训练作业配置示例（8 节点 x 8 卡） |
| `tests/` | 离线单测（不连集群、不联网） |

## 作业配置示例（exp_vla.yaml）

```yaml
job_name: vla-pretrain-pi0-v1
partition: gpu
nodes: 8                      # 8 节点 x 8 卡 = 64 GPU
gpus_per_node: 8
cpus_per_task: 16
time_limit: "72:00:00"

image: registry-vpc.cn-hangzhou.aliyuncs.com/vla/train:0.1   # pyxis/enroot 容器
mounts:                       # CPFS 挂载（数据由 phi-sync 同步好）
  - /mnt/cpfs/vla_dataset_v1:/data
  - /mnt/cpfs/checkpoints:/ckpt
  - /mnt/cpfs/logs:/logs

command: |
  train.py --config configs/pi0_v1.yaml --data /data --ckpt /ckpt/vla-pi0-v1

requeue:
  enabled: true               # 故障自动重提交（Slurm 不原生做，SDK 做）
  max_retries: 3
  resume_from: /ckpt/vla-pi0-v1
```

提交与查询（规划中的 CLI）：

```bash
phi-train submit --config examples/exp_vla.yaml      # 提交，打印 job id
phi-train status <job-id>                            # 查状态
phi-train logs <job-id> --follow                     # 追日志（日志落 CPFS，不 SSH 计算节点）
phi-train cancel <job-id>                            # 取消
```

## 设计要点

1. **声明式 JobSpec**：纯数据、可序列化（`to_dict`/`from_dict`），团队共享训练模板，也是 requeue 重提交的依据。
2. **状态归一**：作业在队列时查 `squeue`，已结束查 `sacct`，统一映射为 `JobStatus`（`PENDING/RUNNING/SUCCEEDED/FAILED/CANCELLED/REQUEUED`），上层不用关心 Slurm 状态差异。
3. **gang scheduling 交给 Slurm**：`--nodes=N` 由 Slurm 保证要么全起要么不起；每节点 1 个 srun 任务，内部 torchrun 拉起 N 个 GPU 进程（`--ntasks-per-node=1 + srun torchrun`）。
4. **日志落 CPFS**：sbatch 的 `--output/--error` 指向 CPFS 日志目录，登录节点即可读，SDK 拉日志无需进计算节点。
5. **故障自动续训（requeue）**：`wait()` 发现 `NODE_FAIL` 等失败状态时，按 `requeue.max_retries` 自动用同一 JobSpec 重提交；训练代码契约：`--resume <dir>` 存在则从最近 checkpoint 续训。
6. **零运行时依赖**：核心链路只用标准库，SSH 走系统 `ssh` 命令（subprocess）；`pyyaml` 为可选 extra（`pip install "phi-train-slurm[yaml]"`）。对齐 phi_data_sdk 的「wheel 自包含、离线可装」原则。
7. **提交前 sanity check**：`submit()` 前校验挂载路径在登录节点存在且数据集目录非空，避免训空数据。

## 开发状态（skeleton）

当前为**空架子**：对象模型、状态映射、脚本渲染、CLI 骨架已就位且可离线测试；`SlurmBackend` 的 SSH 执行与解析为 TODO。待实现清单：

- [ ] `SlurmBackend._run`：subprocess 调 `ssh` 执行远程命令
- [ ] `SlurmBackend.submit`：上传脚本 + `sbatch` + 解析 "Submitted batch job <id>"
- [ ] `SlurmBackend.status`：`squeue` 优先、`sacct` 兜底，按 `job/status.py` 映射归一
- [ ] `JobHandle.wait`：轮询 + requeue 自动续训循环
- [ ] `JobHandle.logs`：SSH `tail -f` CPFS 日志
- [ ] `submitter` CLI 与 `create_backend` 的环境变量配置打通

## 安装与开发

```bash
# 仓库内开发
pip install -e ".[dev,yaml]"

# 跑测试（全部离线）
pytest
```

打包分发：`python -m pip wheel . --no-deps -w dist`，产物为纯 Python wheel。
